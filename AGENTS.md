# danbooru-tag-zh 工程约束

## 项目目标

- 从 Danbooru 官方接口维护可验证、可追溯的标签清单。
- 以 Danbooru Wiki 中可验证的标签语义为首选翻译依据。
- 生成供 Danbooru Masonry 使用的精简简体中文标签映射。
- 数据获取与翻译是两个独立阶段。标签同步完成并验证前，不开始翻译。

## 标签分类与翻译规则

Danbooru 分类编号必须按以下映射处理，不得自行增加或合并分类：

| 编号 | 分类 | 翻译策略 |
| --- | --- | --- |
| 0 | `general` | 翻译 |
| 1 | `artist` | 不翻译，不进入最终翻译文件 |
| 3 | `copyright` | 翻译，优先官方或通行作品名 |
| 4 | `character` | 翻译，优先官方或通行角色名 |
| 5 | `meta` | 翻译 |

- 翻译必须尽可能简短、明确，并保持原标签的有效语义差异。
- 不写解释句、定义、括号补充或“画面中包含”等冗余前缀。
- `copyright` 和 `character` 不得为了缩短而使用不可靠的字面翻译。
- 没有可靠中文结果时保持缺失，不得猜测。只有人工明确确认中文语境也使用原词时，才可将原标签记录为 `original_term`。
- 最终导出必须区分经过人工确认的 `original_term` 与未经翻译的缺失记录。
- 中文候选与最终中文译文使用简体中文；繁体内容不得自动转换成新的候选。
- 英文缩写、型号及专有名称优先采用可靠的中文名称；人工确认中文语境也使用原词时，可保留原文，并按实际用法调整大小写和空格。

## 翻译来源优先级

1. Danbooru Wiki 中可定位、可记录来源的标签语义和专名信息。
2. 项目内经过人工审核的修正。
3. 已获 MIT 授权的 ffdkj 翻译数据，作为独立数据集发布，不与 Wiki 审核结果静默混合。

Danbooru Wiki 通常不是现成的中文翻译表。不能从 Wiki 内容可靠得出简短中文标签时，将记录送入人工审核队列。不得自动调用大模型补齐，也不得把机器生成内容标记为 Wiki 翻译。

Wikipedia 链接必须区分当前 tag 主体与原作、改编来源及其他关联资料。只有 `subject` 页面标题可以参与自动匹配；`related` 和 `unknown` 仅作为底层证据保存。

每条翻译必须保留来源类型、来源 URL、抓取时间和审核状态。Wiki 页面正文与页面元数据属于原始证据，不直接作为最终短译文；从证据提炼出的短译文必须可追溯。

## 阶段顺序

### 第一阶段：获取标签

- 使用 Danbooru 官方 `/tags.json` 接口获取标签事实。
- 默认获取 `post_count >= 10` 的标签，保留五种有效分类。
- 使用稳定的 ID 游标分页；请求应限速，并对限流和临时服务错误退避重试。
- 保存标签 ID、名称、分类、帖子数、创建时间、更新时间和弃用状态。
- 支持失败后从临时数据库继续；完整同步并验证后才能替换正式数据库。
- 此阶段的数据结构不得包含伪造、占位或推断的中文翻译。

### 第二阶段：获取 Wiki 证据

- 只有第一阶段通过记录数、唯一性、分类和 SQLite 完整性验证后才能开始。
- Wiki 获取结果与标签事实分表保存，失败可以独立重试。
- 缓存原始响应，遵守限速，避免重复请求未变化页面。

### 第三阶段：生成与审核翻译

- 按上述来源优先级生成候选短译文。
- `artist` 必须跳过；其他四类分别统计覆盖率。
- 自动规则只能接受高置信度结果，其余进入人工审核队列。
- 最终文件只包含已接受且不等于原标签的翻译。
- `artifacts/ffdkj` 与 `artifacts/wiki-reviewed` 必须独立生成、独立记录 manifest，不得互相补缺或覆盖。

## 文档写作

- README 用于说明个人 GitHub 项目的用途、数据来源和处理方式，不写成产品介绍或研究报告。
- 默认采用最小修改原则，保留原意和已有技术事实；仅为纠正错误、消除歧义或满足实际使用需求补充内容。
- 段落和表格只保留必要且能独立读懂的信息；说明和链接应帮助理解当前内容或完成下一步操作。
- 使用直接、具体、自然的表达，优先写清楚数据从哪里来、项目对数据做了什么。
- 少用“经过确认的”“基于……的”“人工决定”“高覆盖率”等概括或评价性措辞；避免名词堆叠、营销表达和过度润色。
- 不恢复此前因冗余而删除的说明；出现新的事实或使用需求时重新评估。
- README 的“致谢”“鸣谢”或 `Credits` 只写感谢，不扩写成授权声明或取证结论。
- `License`、版权说明和法律风险说明可以使用正式、准确的许可措辞。

## 工程要求

- 优先正确性、可追溯性和最小改动，不引入不必要依赖。
- 网络客户端必须可在测试中替换；单元测试不得依赖在线 Danbooru。
- SQLite 写入使用事务；发布数据前执行 `PRAGMA integrity_check`。
- 原始数据库、缓存和临时文件不得提交到 Git。两套精简发布产物及 manifest 可以作为明确审核过的发布文件纳入版本控制。
- README、配置、CLI 和测试必须与当前已完成阶段一致，不得提前宣称后续翻译功能可用。
- 修改后运行 Ruff、Mypy、Pytest 及 `git diff --check`。
- 未经明确要求，不提交、打标签、推送或发布数据。
