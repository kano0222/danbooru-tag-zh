# danbooru-tag-zh 协作约定

## 目标与授权

- 维护可追溯的 Danbooru 标签数据，生成供 Danbooru Masonry 使用的精简简体中文映射。优先正确性、可维护性和最小充分改动。
- 可以反驳需求，但应给出事实、影响和可执行的替代方案，不盲从，也不擅自改变目标。观点和方案讨论不自动授权实现；明确要求修改后，自主完成目标内必要的工作。
- 只读检查、已授权范围内的可回滚编辑和相关验证可自主进行。提交、打标签，以及改变远端、第三方服务或用户持久化数据的操作，必须在明确授权范围内；本地修改请求不自动授权推送、发布、对外评论、修改远端配置或删除用户数据。
- 已有授权持续有效，不要求逐步重复确认。自动更新的实施与启用分别以用户授权为准；明确授权持续自动发布后，满足既定条件的运行无需逐次确认。

## 数据边界

- 标签事实来自 Danbooru 官方接口，默认范围为 `post_count >= 10`。保留分类编号：`0 general`、`1 artist`、`3 copyright`、`4 character`、`5 meta`；画师标签不进入翻译产物。
- `ffdkj` 与 `wiki-reviewed` 是独立数据集，分别生成产物和 manifest，不静默混合、补缺或覆盖。ffdkj 构建不依赖 Wiki 审核流程完成。
- Wiki 路线以可追溯的标签语义和专名信息生成候选，人工修正优先于自动候选。Wikipedia 只有主体对应的 `subject` 页面参与自动匹配，关联或不确定页面仅作证据；不得把机器生成内容标记为 Wiki 翻译，也不擅自调用大模型补齐。
- 译文简短、明确，保留有效语义差异；作品和角色优先使用官方或通行名称，不猜测、不堆叠解释。候选和译文使用简体中文，不自动将繁体内容转成新候选。
- 无可靠译文时留缺；人工确认沿用原词的记录保留 `original_term` 状态，与缺失记录区分。最终映射只导出已接受且不等于原标签的翻译，保留现有括号规范化规则。
- 按来源保留足以复现的记录：ffdkj 固定上游提交及校验值，Wiki 候选保留来源、抓取时间和审核状态。原始证据不直接当作最终译文。
- 校验失败不得替换已发布数据；候选数据与有效基线分开。发布产物、manifest 和来源记录须一致，格式校验通过不等于译文语义正确。

## 执行与验证

- 修改前检查工作树及相关实现，保护用户已有改动。复用现有工具链、数据结构和校验能力，不顺带重构或扩展功能；新增依赖应说明必要性，涉及新服务、费用或权限时先确认。
- 自主安排步骤和验证范围，不机械套用阶段顺序。只补足当前目标需要的能力，不为假设风险增加评分体系、审批流程、配置项或架构。
- 网络客户端可在测试中替换，单元测试不依赖在线 Danbooru 或上游服务。批量请求遵守限速并处理临时失败；SQLite 写入使用事务，候选数据库使用前检查完整性。
- 验证与改动风险相称，使用现有 Ruff、Mypy、Pytest 等检查；纯文档修改核对内容和 `git diff --check` 即可。修复后重跑受影响的检查，避免重复执行已覆盖且未受影响的检查；未验证的结果如实说明。
- 经授权发布时，检查现有流程覆盖范围并补齐必要的数据校验和测试，只提交目标内的产物及配套文件。原始数据库、缓存和临时文件不进入 Git；发布后核对实际结果，不覆盖历史标签或强推。

## 文档与沟通

- README 直接说明用途、用法、数据来源和处理方式，保留必要事实，不写营销表达、空泛评价或无助于使用的实现细节。致谢只表达感谢，许可措辞放在相应说明中。
- 改动涉及文档、配置或 CLI 行为时，相关中英文说明、默认值和测试与当前实现一致，不作无关联动，不宣称未完成的功能可用。
- 回复先给结论，只说明实际改动、关键依据、验证结果和影响当前决策的限制。方案给出足以执行的内容，不为凑完整性罗列无关风险或后续工作。
